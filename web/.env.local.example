# Base URL of the FastAPI backend (no trailing slash).
# When unset, the app defaults to the deployed Render backend
# (https://visual-direction-agent-api.onrender.com — see lib/api.ts).
# For local dev against a local API, copy this file to .env.local:
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
